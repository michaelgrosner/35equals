// Re-export everything from column-defs for backwards compatibility.
export {
  TAG_NAMES,
  DEFAULT_VISIBLE_TAGS,
  indexColumn,
  makeTagColumn,
  defaultColumns,
} from '@/lib/column-defs';
export type { GridRow } from '@/lib/column-defs';
